extends ./layouts/main.pug
include mixins/videoBlock
block content
    .videos
        each vid in video
            +videoBlock({
                title:vid.title,
                views:vid.views,
                videoFile:vid.videoFile
            })